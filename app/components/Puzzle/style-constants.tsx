import { css } from "styled-components";

export const easeFunction = css`cubic-bezier(.53,0,.3,1)`;

export const zIndexes = {
    background: 0,
    view: 10,
    viewButtonElements: 20,
    viewTopBar: 25,
    editModeOverlayBackground: 30,
    editModeOverlay: 40,
    editModeControls: 50,
};
